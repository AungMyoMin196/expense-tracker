pre-commit:
  parallel: false
  commands:
    formatter:
      glob: '*.dart'
      run: flutter format {staged_files} && git add .
    linter:
      glob: '*.dart'
      run: dart fix --apply lib && git add .
    analyzer:
      glob: '*.dart'
      run: flutter analyze